Kanban is a fully functional task management application.

To run locally:

- cd server
- npm install
- npm start will run your backend (node.js) and your client (react) concurrently.
- For client only: npm run client
- For backend only: npm run server
